paho-mqtt==2.1.0
python-dotenv==1.0.1
psycopg[binary]==3.2.10
kubernetes==32.0.1
python-dotenv
paho-mqtt

# Kubernetes POC CLI prerequisites; install these as standalone binaries, not with pip:
# kubectl
# Helm 3
# kind
